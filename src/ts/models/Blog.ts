// Blog class

export class Blog {
    name: string;
    id: number;
    author: string;
  
    constructor(
        name: string,
        id: number,
        author: string,
    ) {
      this.name = name;
      this.id = id;
      this.author = author;
    }
  }
